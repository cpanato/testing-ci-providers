name: Test Run

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: build
        run: |
          echo Sleeping...
          sleep $[ (50 + $RANDOM % 10 )  + 180 ]s
          echo DONE
