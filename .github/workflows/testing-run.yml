name: Test Run

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: build
        run: |
          echo Sleeping...
          sleep $[ (30 + $RANDOM % 10 )  + 1 ]s
          echo DONE
