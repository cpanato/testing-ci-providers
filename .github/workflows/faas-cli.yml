name: faas-cli

on:
  pull_request:
  push:

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: cpanato/faas-cli-installer@v1.0.0
      - name: check version
        run:  faas-cli version
