name: test

on:
  pull_request_target:
    branches:
      - 'main'
    types:
      - labeled

jobs:
  test:
    if: contains(github.event.pull_request.labels.*.name, 'ok-to-test')

    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
